message の流れ

1
クライアントからメッセージ送信： 
ユーザーがIRCクライアントでメッセージを入力し、それがIRCプロトコルに従って形成され、
サーバーへ送信されます。各クライアントはそれぞれが独自のメッセージを生成しますが、
それは一般的にはただの文字列で、特にメッセージクラスを持つとは限りません。

2. server で処理 メッセージをパースしてmap に格納
サーバーでのメッセージ受信とパース： サーバーはメッセージを受信し、それをIRCプロトコル
に基づいてパースします。パースの結果は、通常、コマンド（文字列）とそのパラメータ（文字列のリスト）に分解されます。
これらは、例えば、構造体やクラス、またはその他の適切なデータ構造に格納されます。

3. server で処理　コマンド　から各コマンドの呼び出し
コマンドの実行： サーバーはパースされたコマンドを用いて、
関連する処理を行う関数（またはメソッド、または関数ポインタ）を呼び出します。
マップ（コマンド名をキーとし、関数ポインタを値とする）によって
管理されます。

4
コマンド処理： 呼び出された関数は、コマンドに応じた処理を行います。
例えば、"JOIN"コマンドの場合、指定されたユーザーを指定されたチャンネルに追加します。また、"PRIVMSG"コマンドの場合、
指定されたメッセージを指定されたチャンネルまたはユーザーに転送します。
