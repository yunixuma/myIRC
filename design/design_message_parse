コロンによるプレフィックス:
 IRCプロトコルでは、
コロン（:）が特別な意味を持つことがあります。メッセージの最後のパラメータがコロンで始まる場合、
その後のすべてのテキスト（空白を含む）がそのパラメータの一部となります。上記のコードでは、
この挙動を考慮していないため、このタイプのメッセージを正しくパースできません。


PRIVMSG #channel :Hello, everyone! How are you today?
通常2つの主要なパラメータがあります。
最初のパラメータはメッセージの宛先（ユーザーまたはチャンネル）であり、2つ目のパラメータは実際のメッセージ内容です。

JOIN #channelname


コマンドとパラメータの分離:
コードはコマンドとパラメータを分離していますが、
IRCメッセージの一般的な構造に基づいていないため、パラメータの解釈に誤解が生じる可能性があります。

エラーハンドリング:
 不正な形式のメッセージが来た場合のエラーハンドリングが存在していません。
メッセージの形式が正しくない場合、どのように対処するかを考慮する必要があります。

特別なコマンド: 特定のコマンドは特別な構造を持つことがあるため、
一般的なトークン化ロジックだけでは処理できない場合があります。